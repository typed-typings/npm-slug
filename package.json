{
  "private": true,
  "scripts": {
    "lint": "tslint slug.d.ts",
    "bundle": "typings bundle . -o bundle.d.ts",
    "test-spec": "ts-node test.ts",
    "test": "npm run lint && npm run bundle && npm run test-spec"
  },
  "devDependencies": {
    "slug": "^0.9.1",
    "ts-node": "^0.7.3",
    "tslint": "^3.10.2",
    "tslint-config-typings": "^0.2.3",
    "typescript": "^1.8.10",
    "typings": "^1.0.4"
  }
}
